#!/bin/sh

pushd $(dirname $0)
cd ..
BASE_DIR=$(pwd)
popd

# If there are classfiles in the build directory, use those. Otherwise, use the classes in the bootstrap
# directory.

java -classpath $BASE_DIR/build:$BASE_DIR/bootstrap/javacc.jar/$BASE_DIR/bootstrap/freemarker.jar javacc.Main "$@"




