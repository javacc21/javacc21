#!/bin/sh

# If there is a javacc.jar in the base directory, use that. Otherwise use the bootstrap jarfile in the bin directory.

DIR=$(dirname $0)
JAVACC_JAR=$DIR/javacc.jar

if test -f "$DIR/../javacc.jar" ; then
   JAVACC_JAR="$DIR/../javacc.jar"
fi

java -jar "$JAVACC_JAR" "$@"

